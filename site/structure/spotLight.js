var spotLight = new THREE.SpotLight( 0xecf0f1 );
spotLight.position.set( 0, 1800, 0 );
spotLight.angle = Math.PI / 1;
spotLight.intensity = 1.5;
spotLight.penumbra = 0;
spotLight.decay = 1;
spotLight.angle = 0.5;
spotLight.distance = 5000;
spotLight.castShadow = true;
spotLight.shadow.mapSize.width = 1024;
spotLight.shadow.mapSize.height = 1024;
spotLight.shadow.camera.near = 10;
spotLight.shadow.camera.far = 200;
scene.add( spotLight );

var spotLight2 = new THREE.SpotLight( 0xfdcb6e );
spotLight2.position.set( 25, 200, -10 );
spotLight2.angle = Math.PI / 1;
spotLight2.intensity = 0.5;
spotLight2.penumbra = 0.8;
spotLight2.decay = 0.5;
spotLight2.distance = 250;
spotLight2.castShadow = true;
spotLight2.shadow.mapSize.width = 1024;
spotLight2.shadow.mapSize.height = 1024;
spotLight2.shadow.camera.near = 10;
spotLight2.shadow.camera.far = 200;
scene.add( spotLight2 );

var spotLight3 = new THREE.SpotLight( 0xfdcb6e );
spotLight3.position.set( 450, 200, -10 );
spotLight3.angle = Math.PI / 1;
spotLight3.intensity = 0.5;
spotLight3.penumbra = 0.8;
spotLight3.decay = 0.5;
spotLight3.distance = 250;
spotLight3.castShadow = true;
spotLight3.shadow.mapSize.width = 1024;
spotLight3.shadow.mapSize.height = 1024;
spotLight3.shadow.camera.near = 10;
spotLight3.shadow.camera.far = 200;
scene.add( spotLight3 );

var spotLight4 = new THREE.SpotLight( 0x74b9ff );
spotLight4.position.set( -575, 300, 100 );
spotLight4.angle = Math.PI / 1;
spotLight4.intensity = 0.8;
spotLight4.penumbra = 0.8;
spotLight4.decay = 0.5;
spotLight4.distance = 400;
spotLight4.castShadow = true;
spotLight4.shadow.mapSize.width = 1024;
spotLight4.shadow.mapSize.height = 1024;
spotLight4.shadow.camera.near = 10;
spotLight4.shadow.camera.far = 200;
scene.add( spotLight4 );
